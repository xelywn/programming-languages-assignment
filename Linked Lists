#include <stdio.h>
#include <stdlib.h>

struct Node {
    int data;
    struct Node *next;
};

int main() {
    struct Node *head = NULL, *temp;

    head = malloc(sizeof(struct Node));
    head->data = 10;
    head->next = NULL;

    temp = malloc(sizeof(struct Node));
    temp->data = 5;
    temp->next = head;
    head = temp;

    temp = head;
    head = head->next;
    free(temp);

    temp = head;
    while (temp != NULL) {
        printf("%d ", temp->data);
        temp = temp->next;
    }
}
